import * as React from 'react'
import { Button } from 'antd';

export const ClientList = (props:any) => {
    return <div>
        Client List
        <Button type='primary'>deconect</Button>
    </div>
}